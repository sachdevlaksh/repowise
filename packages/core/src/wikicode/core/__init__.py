"""WikiCode core package.

Provides the ingestion pipeline, dependency graph builder, and documentation
generation engine. This is the heart of WikiCode — all other packages depend on it.

Namespace package: wikicode.core is part of the wikicode namespace.
"""

__version__ = "0.1.0"
