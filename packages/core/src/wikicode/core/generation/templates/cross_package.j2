You are documenting the inter-package dependency: `{{ ctx.source_package }}` → `{{ ctx.target_package }}`.

- **Source**: `{{ ctx.source_package }}` (mean PageRank: {{ "%.4f"|format(ctx.source_pagerank_mean) }})
- **Target**: `{{ ctx.target_package }}` (mean PageRank: {{ "%.4f"|format(ctx.target_pagerank_mean) }})
- **Coupling**: {{ ctx.coupling_strength }} files in source depend on target

## Files Crossing the Boundary
{% for f in ctx.boundary_files %}
- `{{ f }}`
{% endfor %}

## Public API Used from `{{ ctx.target_package }}`
{% for sym in ctx.used_symbols %}
- `{{ sym }}`
{% endfor %}

Generate a complete wiki page with sections:
## Overview, ## Integration Points, ## API Dependency Summary, ## Coupling Risk.
