You are generating an architecture diagram for the repository `{{ ctx.repo_name }}`.

## Repository Architecture

**Nodes** ({{ ctx.nodes | length }} files):
{% for node in ctx.nodes %}
- `{{ node }}`
{% endfor %}

**Edges** ({{ ctx.edges | length }} dependencies):
{% for src, dst in ctx.edges %}
- `{{ src }}` → `{{ dst }}`
{% endfor %}

**Communities** ({{ ctx.communities | length }} detected):
{% for community_id, members in ctx.communities.items() %}
- Community {{ community_id }}: {{ members | join(", ") }}
{% endfor %}

**Strongly Connected Components** (cycles):
{% for scc in ctx.scc_groups %}
- {{ scc | join(", ") }}
{% endfor %}

Note: Showing top {{ ctx.nodes | length }} files by PageRank.
Use module pages for full per-file coverage.

You MUST include a fenced mermaid block with graph TD showing the key dependencies.
The mermaid diagram should use short node labels for readability.

Generate a complete wiki page following the sections above.
