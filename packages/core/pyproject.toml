# ---------------------------------------------------------------------------
# repowise-core — per-package config (packaging is handled by root pyproject.toml)
# ---------------------------------------------------------------------------

[tool.hatch.build.targets.wheel]
# Retained for local hatchling dev builds if needed
packages = ["src/repowise"]

[tool.uv]
package = true
