# ---------------------------------------------------------------------------
# repowise-server — per-package config (packaging is handled by root pyproject.toml)
# ---------------------------------------------------------------------------

[tool.hatch.build.targets.wheel]
packages = ["src/repowise"]

[tool.uv]
package = true

[tool.uv.sources]
repowise-core = { workspace = true }
